spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/digital_airport_db
    username: postgres
    password: Test1234=
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

springdoc:
  swagger-ui:
    path: /swagger
